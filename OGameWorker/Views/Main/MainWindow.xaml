<Window
    x:Class="OGameWorker.Views.Main.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:OGameWorker.Views.Main"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="525"
    Height="350"
    mc:Ignorable="d">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid DataContext="{Binding ResourcesViewModel}">
        <StackPanel Orientation="Horizontal">
            <TextBox
                x:Name="Metal"
                BorderThickness="0"
                IsReadOnly="True"
                Text="{Binding Metal.Amount, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox
                x:Name="Crystal"
                BorderThickness="0"
                IsReadOnly="True"
                Text="{Binding Crystal.Amount, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox
                x:Name="Deuterium"
                BorderThickness="0"
                IsReadOnly="True"
                Text="{Binding Deuterium.Amount, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</Window>